# autoresearch

A Mars prompt package for autonomous research coordination.

The first version contains:

- `skills/autoresearch` — reusable method for turning research questions into campaigns, experiments, result triage, and durable artifacts.
- `agents/autoresearch-lead` — a thin coordination agent that loads the skill and keeps implementation delegated.

Install or sync it through Mars/Meridian in a downstream project.
